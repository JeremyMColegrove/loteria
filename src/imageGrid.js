import React, { useState } from 'react';

function ImageGrid({ images }) {
  // State to store the revealed card
  const [revealedCard, setRevealedCard] = useState(null);

  return (
    <div className="grid-container">
        
    </div>
  )
}

export default ImageGrid;